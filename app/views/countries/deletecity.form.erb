<h1>Sélectionner une ville à supprimer</h1>

<% @cities.each do |city| %>
  <div class='image'>
    <%= link_to image_tag('cities/' + city.name + '.jpg', height: '200px'), city, method: :delete %>
  </div>
<% end %>
